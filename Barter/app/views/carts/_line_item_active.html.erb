<div id="cart-item-<%= item.id %>">
  <%= link_to image_tag(item.textbook.img_url), textbook_path(item.textbook) %>
 <% if item.user_book.sold %> 
  <p>This item is no longer available.</p>
  <p>Seller: <%=item.seller.first_name %> </p>
  <p>Condition: <%=item.user_book.condition.name%></p>
  <p class="remove-item" line-item-id="<%= item.id %>">delete</p>
 <% else %>
  <p>Seller: <%=item.seller.first_name %> </p>
  <p>Condition: <%=item.user_book.condition.name%></p>
  <% if item.user_book.custom_price %>
    <p>Price: <%=item.user_book.custom_price %> karma points</p>
  <% else %>
    <p>Price: <%=item.user_book.current_price.round(2) %> karma points</p>
  <% end %>
  <p class="remove-item" line-item-id="<%= item.id %>">delete</p>
 <% end %>
</div>