<%=@cart.user.first_name%> Cart

<%= render partial: "user_book", collection: @cart.user_books, as: :item %>

<%= form_tag(cart_path(@cart), method: :patch) do %>
  <%= submit_tag "Buy Books Now"%>
<% end %>
