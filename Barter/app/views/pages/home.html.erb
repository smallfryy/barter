<div class="ui inverted vertical masthead center aligned segment">
  <div class="ui text container">
     <h1 class="ui inverted header">Welcome to Bibliotech</h1><br>

  <div class="ui form">
  <p><%= form_tag(textbooks_path, method: "get",) do %>
  <i><%= label_tag(:query, "search for a textbook:") %></i>
  <%= text_field_tag(:query) %><p>
  <%= submit_tag("Search", class: "ui huge primary button" ) %></p>
  <% end %>
  </div>
  </div>
</div>

